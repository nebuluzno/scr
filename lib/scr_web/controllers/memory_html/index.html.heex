<div class="container fade-in">
  <div class="page-header">
    <h1>💾 Memory Storage</h1>
    <p class="subtitle">Persistent ETS-based storage for tasks and agent states</p>
  </div>
  
  <div class="memory-stats">
    <div class="stat-card">
      <div class="stat-icon">📋</div>
      <div class="stat-value"><%= length(@tasks) %></div>
      <div class="stat-label">Tasks Stored</div>
    </div>
    <div class="stat-card">
      <div class="stat-icon">🤖</div>
      <div class="stat-value"><%= length(@agents) %></div>
      <div class="stat-label">Agent States</div>
    </div>
  </div>
  
  <div class="memory-grid">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title">📋 Tasks</h3>
        <span class="badge badge-info"><%= length(@tasks) %> items</span>
      </div>
      <div class="card-body">
        <%= if @tasks == [] do %>
          <div class="empty-state">
            <div class="empty-state-icon">📭</div>
            <p>No tasks stored</p>
          </div>
        <% else %>
          <div class="memory-list">
            <%= for task <- @tasks do %>
              <div class="memory-item">
                <div class="memory-header">
                  <span class="memory-id"><%= task.task_id %></span>
                  <span class={"badge " <> status_badge_class(task.status)}><%= task.status %></span>
                </div>
                <div class="memory-content"><%= String.slice(to_string(task.description), 0, 100) %><%= if String.length(to_string(task.description)) > 100, do: "..." %></div>
                <div class="memory-meta">type: <%= task.type %></div>
                <div class="memory-meta">trace_id: <%= task.trace_id || "n/a" %></div>
                <div class="memory-meta">parent_task_id: <%= task.parent_task_id || "n/a" %></div>
                <div class="memory-meta">subtask_id: <%= task.subtask_id || "n/a" %></div>
                <div class="memory-meta">updated_at: <%= format_datetime(task.updated_at) %></div>
                <%= if task.has_result do %>
                  <div class="memory-result"><strong>Result:</strong> available</div>
                <% end %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
    
    <div class="card">
      <div class="card-header">
        <h3 class="card-title">🤖 Agent States</h3>
        <span class="badge badge-info"><%= length(@agents) %> items</span>
      </div>
      <div class="card-body">
        <%= if @agents == [] do %>
          <div class="empty-state">
            <div class="empty-state-icon">👻</div>
            <p>No agent states stored</p>
          </div>
        <% else %>
          <div class="memory-list">
            <%= for agent <- @agents do %>
              <div class="memory-item">
                <div class="memory-header">
                  <span class="memory-id"><%= agent.agent_id %></span>
                  <span class={"badge " <> status_badge_class(agent.status)}><%= agent.status %></span>
                </div>
                <div class="memory-meta">
                  <span>Type: <%= agent.agent_type %></span>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  
  <div class="section">
    <h2>📖 About Memory Storage</h2>
    <div class="info-cards">
      <div class="info-card">
        <h4>⚡ ETS-Based</h4>
        <p>In-memory storage using Erlang Term Storage for fast access</p>
      </div>
      <div class="info-card">
        <h4>🔄 Persistent</h4>
        <p>Survives agent crashes and restarts via supervision</p>
      </div>
      <div class="info-card">
        <h4>🔍 Queryable</h4>
        <p>All stored data can be retrieved by agents during execution</p>
      </div>
    </div>
  </div>
  
  <div class="actions-bar">
    <a href="/" class="btn btn-secondary">← Back to Dashboard</a>
  </div>
</div>
